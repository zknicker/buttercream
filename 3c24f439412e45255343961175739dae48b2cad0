Correction: the claim that the pin test exercises 'a different path from the one that printed it' is too strong. themeCssVariables and serializeTheme both walk variablePaths and call getThemeValue and serializeValue, so only the extraction and the token count are independent -- a bug inside serializeValue produces a file that both tests accept. The independent anchor is the hand-written shape coverage in css.test.ts. Recorded in docs/adr/0008-theming-pipeline.md rather than amended.
