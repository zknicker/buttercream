---
"@buttercream/react": minor
"@buttercream/styles": minor
---

Add a Sidebar compound component adapted from shadcn/ui's Base UI sidebar: provider state with
cookie persistence and a mod+B toggle, offcanvas and icon collapse modes, floating and inset
variants, a drag rail, grouped menus with badges, actions, sub-menus and loading skeletons, a
mobile drawer, and collapsed-rail tooltips — styled by a public sidebar BEM block whose colours
all derive from theme roles.
